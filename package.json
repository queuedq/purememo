{
  "name": "purememo",
  "version": "0.1.0",
  "description": "A local storage memo app made with PureScript",
  "main": "index.js",
  "private": true,
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "npm-run-all -p serve watch",
    "serve": "serve dist",
    "build": "spago bundle-app --to dist/app.js",
    "watch": "spago bundle-app --to dist/app.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "author": "queued37",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "psc-package": "^3.0.1",
    "pulp": "^12.4.2",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.7.3",
    "serve": "^11.0.0"
  },
  "dependencies": {
    "decimal.js": "^10.2.0"
  }
}
